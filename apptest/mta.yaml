_schema-version: "3.2"
ID: cltestapptest
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: cltestapptest-destination-content
  type: com.sap.application.content
  requires:
  - name: cltestapptest-destination-service
    parameters:
      content-target: true
  - name: cltestapptest-repo-host
    parameters:
      service-key:
        name: cltestapptest-repo-host-key
  - name: cltestapptest-uaa
    parameters:
      service-key:
        name: cltestapptest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: cltestapptest_html_repo_host
          ServiceInstanceName: cltestapptest-html5-srv
          ServiceKeyName: cltestapptest-repo-host-key
          sap.cloud.service: cltestapptest
        - Authentication: OAuth2UserTokenExchange
          Name: cltestapptest_uaa
          ServiceInstanceName: cltestapptest-xsuaa-srv
          ServiceKeyName: cltestapptest-uaa-key
          sap.cloud.service: cltestapptest
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: cltestapptest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cltestapptest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cltestapptest.zip
      name: cltestapptest
      target-path: resources/
- name: cltestapptest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: cltestapptest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cltestapptest-destination-service
    service-plan: lite
- name: cltestapptest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: cltestapptest-xsuaa-srv
    service-plan: application
- name: cltestapptest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cltestapptest-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
